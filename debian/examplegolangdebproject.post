set -ex

# echo 1
chown -R orangepi:orangepi /etc/nicknameproject/
chmod 664 /etc/nicknameproject/pipelinego/config.toml

FILE=/etc/nicknameproject/opayterminal/config.json
if [ -f "$FILE" ]; then
    echo "$FILE exists."
else
    echo "$FILE does not exist."
    mkdir /etc/nicknameproject/opayterminal/
    cp /usr/lib/opay-terminal/resources/app/config/default.json \
        /etc/nicknameproject/opayterminal/config.json
fi
